# todo note commands

- Chose to do a single cluster since multi cluster did not work

- To init the cluster: "docker swarm init" 
- to deploy "docker stack deploy -c docker-compose.yml sbd3ue". 
  
- I would like to never use Kubernetes again this put me through hell